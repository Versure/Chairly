We are going to set up a new feature: $ARGUMENTS

Before you implement anything:

1. Does a spec already exist in docs/specs/ for this feature?
   - If yes: read the spec and ask clarifying questions if anything is unclear
   - If no: help me write the spec by walking through the template in
     docs/specs/_template.md — ask me questions per section

2. After reading/writing the spec, walk through this checklist:
   - Which existing entities/code will be reused?
   - Are there dependencies on other features that don't exist yet?
   - Are there decisions not covered in the spec? (ask them as questions)

3. Create an implementation plan in steps and let me review it

4. Wait for my go-ahead per step