export interface ClientOption {
  id: string;
  firstName: string;
  lastName: string;
}

export interface StaffMemberOption {
  id: string;
  firstName: string;
  lastName: string;
}

export interface ServiceOption {
  id: string;
  name: string;
  duration: string;
  price: number;
}
