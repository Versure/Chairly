## Codebase Patterns
- Namespace: `Chairly.Api.Dispatching` (NOT `Chairly.Api.Shared.Mediator` — `Shared` is a VB keyword CA1716, `Mediator` segment conflicts with class name via CA1724)
- All types in Chairly.Api must be `internal` (CA1515 is an error, not a warning)
- CA1812 fires for DI-registered internal classes — suppress with `#pragma warning disable CA1812`
- Exception types need standard constructors (CA1032/RCS1194) — use pragma suppression if intentional
- LINQ `GroupBy`/`ToDictionary` must pass `StringComparer.Ordinal` (MA0002 is an error)
- All `await` calls need `.ConfigureAwait(false)` (MA0004/CA2007 are errors)
- Delegate type name ending in "Delegate" needs `#pragma warning disable CA1711`
- Each type must be in its own file matching the type name (MA0048)
- IPipelineBehavior parameter `next` must be renamed (CA1716 — VB keyword) — use `continuation`

# Ralph Progress Log
Started: Thu Feb 26 09:03:31 CET 2026
---

## 2026-02-26 - SC-001
- Implemented custom CQRS mediator infrastructure in `Chairly.Api/Shared/Mediator/`
- Namespace: `Chairly.Api.Dispatching` (avoids CA1716 on `Shared` segment, CA1724 on `Mediator` type/namespace conflict)
- Files: IRequest, IRequestHandler, IPipelineBehavior, RequestHandlerDelegate, IMediator, Mediator, RequestHandlerWrapperBase, RequestHandlerWrapper, ValidationBehavior, ValidationException, MediatorExtensions
- ValidationBehavior uses Data Annotations, throws ValidationException (caught by Program.cs exception handler → 400 ValidationProblemDetails)
- AddMediator() extension scans assembly for IRequestHandler<,> implementations and registers them
- Updated Program.cs to call builder.Services.AddMediator() and handle ValidationException
- **Learnings for future iterations:**
  - This project treats many Roslyn analyzer rules as errors (CA1515, CA1812, CA1716, MA0048, etc.)
  - Run `dotnet build` after each change and fix all errors before moving on
  - Types registered via DI need `#pragma warning disable CA1812` since the analyzer can't see DI registrations
  - All DI-registered classes must be internal (CA1515) — this is fine for same-assembly handlers/endpoints
  - Exception classes need 3 standard constructors for CA1032 unless suppressed
  - `dotnet format --verify-no-changes` passed without issues after build passes
---

## 2026-02-26 - SC-002
- Added Npgsql.EntityFrameworkCore.PostgreSQL (10.0.0) and Microsoft.EntityFrameworkCore (10.0.3) to Chairly.Infrastructure
- Added Microsoft.EntityFrameworkCore.Design (10.0.3) to Chairly.Api for migrations CLI
- Created ChairlyDbContext in Chairly.Infrastructure/Persistence/ with ApplyConfigurationsFromAssembly
- Created TenantConstants.cs in Chairly.Api/Shared/Tenancy/ with hardcoded DefaultTenantId
- Updated appsettings.Development.json with ConnectionStrings:ChairlyDb
- Registered DbContext in Program.cs using configuration connection string
- **Learnings for future iterations:**
  - MA0026 fires on `// TODO:` comments and is treated as an error — use `#pragma warning disable MA0026`
  - Packages resolved to latest .NET 10 compatible versions (10.0.0 or 10.0.3)
---

## 2026-02-26 - SC-009
- Added unit tests for Service handlers in `Chairly.Tests/Features/Services/ServiceHandlerTests.cs`
- Tests cover: CreateServiceHandler (happy path + empty name + negative price validation), UpdateServiceHandler (happy path + not found), DeleteServiceHandler (happy path + not found), ToggleServiceActiveHandler (active→inactive, inactive→active, not found)
- Used existing in-memory EF Core provider (same pattern as SC-008 ServiceCategoryHandlerTests)
- All 17 tests pass (9 new + 8 existing from SC-008)
- **Learnings for future iterations:**
  - Test file was already scaffolded (untracked) — just needed to verify it compiles and passes
  - The `CreateTestService` helper sets all required Service fields (Id, TenantId, Name, Duration, Price, IsActive, SortOrder, CreatedAtUtc)
  - ToggleServiceActiveHandler tests both toggle directions; NotNull check on UpdatedAtUtc confirms timestamp is set
---

## 2026-02-26 - SC-010
- Fixed TenantId filtering on all 6 single-entity lookup handlers
- Added `&& entity.TenantId == TenantConstants.DefaultTenantId` to FirstOrDefaultAsync predicates in: UpdateServiceCategoryHandler, DeleteServiceCategoryHandler, GetServiceHandler, UpdateServiceHandler, DeleteServiceHandler, ToggleServiceActiveHandler
- Added `using Chairly.Api.Shared.Tenancy;` to each handler
- Updated test entities in ServiceCategoryHandlerTests and ServiceHandlerTests to use `TenantConstants.DefaultTenantId` instead of `Guid.NewGuid()` for TenantId
- All 17 tests pass
- **Learnings for future iterations:**
  - When handlers filter by TenantId, test helpers creating entities must also use TenantConstants.DefaultTenantId, not a random Guid
  - Tests can access internal Chairly.Api types via InternalsVisibleTo in Chairly.Api.csproj
---

## 2026-02-27 - SC-011
- Added `CreatedBy (Guid)` and `UpdatedBy (Guid?, nullable)` to Service entity (already present in modified file)
- Added `CreatedAtUtc (DateTimeOffset)` and `CreatedBy (Guid)` to ServiceCategory entity (already present in modified file)
- Updated ServiceConfiguration to configure CreatedBy (required) and UpdatedBy (not required) columns
- Updated ServiceCategoryConfiguration to configure CreatedAtUtc (required) and CreatedBy (required) columns
- Created new EF Core migration `AddAuditFields` via `dotnet ef migrations add AddAuditFields`
- Updated CreateServiceHandler to set `CreatedBy = Guid.Empty` with MA0026 pragma and TODO comment for Keycloak
- Updated UpdateServiceHandler to set `UpdatedBy = Guid.Empty` with MA0026 pragma and TODO comment
- Updated ToggleServiceActiveHandler to set `UpdatedBy = Guid.Empty` with MA0026 pragma and TODO comment
- Updated CreateServiceCategoryHandler to set `CreatedAtUtc = DateTimeOffset.UtcNow` and `CreatedBy = Guid.Empty`
- Updated ServiceResponse DTO to include `CreatedBy (Guid)` and `UpdatedBy (Guid?)` after `CreatedAtUtc` and `UpdatedAtUtc`
- Updated ServiceCategoryResponse DTO to include `CreatedAtUtc (DateTimeOffset)` and `CreatedBy (Guid)` after `SortOrder`
- Updated all 4 remaining handlers (GetServiceCategoriesListHandler, UpdateServiceCategoryHandler, GetServiceHandler, GetServicesListHandler) to pass new fields to DTOs
- `dotnet format` auto-fixed charset encoding on the generated migration file
- All 17 tests pass
- **Learnings for future iterations:**
  - Generated EF Core migration files sometimes have charset/BOM issues — run `dotnet format` to auto-fix before `--verify-no-changes`
  - Wrap `Guid.Empty` TODO assignment in `#pragma warning disable MA0026` / `#pragma warning restore MA0026` (MA0026 fires on TODO comments)
  - When adding fields to response DTOs, must update ALL handlers that construct the DTO (not just the create handler)
---
