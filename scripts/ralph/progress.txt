# Ralph Progress Log
Started: Fri Feb 27 14:15:21 CET 2026
---

## 2026-03-02 - SCF-015
- Final verification: all lint, test, and build checks pass with zero warnings
- Verified all components: OnPush change detection, standalone: true, chairly- prefix
- Verified: no any types, no console statements, explicit return types on all functions
- Verified: signal-based APIs throughout (input(), output(), viewChild(), computed()) — no decorators
- Verified: no cross-domain imports, Sheriff boundaries respected
- Verified: all barrel exports in place (feature/index.ts, ui/index.ts, data-access/index.ts, util/index.ts)
- All 119 tests pass (108 chairly-lib + 10 shared-lib + 1 chairly app)
- Files changed: prd.json, progress.txt only (no code changes needed)

---

## 2026-03-02 - SCF-014
- services.routes.ts created in libs/chairly/src/lib/services/feature/
- Route provides ServiceStore, ServiceCategoryStore at route level (scoped to /services)
- ServiceApiService, ServiceCategoryApiService are providedIn:'root' so don't strictly need route providers, but added for explicitness
- libs/chairly/src/index.ts updated to export servicesRoutes from the feature barrel
- feature/index.ts updated to also export servicesRoutes
- app.routes.ts updated: default '' redirects to /services, lazy route via import('@org/chairly-lib').then(m => m.servicesRoutes)
- sheriff.config.ts updated: added 'libs/chairly/src': ['chairly-lib'] tag and dep rules for 'root' and 'chairly-lib'
- Fixed pre-existing build error in util/index.ts: changed `export { Interface }` to `export type { Interface }` for interface re-exports
- All lint + test + build checks pass
- Files changed: services.routes.ts, feature/index.ts, libs/chairly/src/index.ts, app.routes.ts, sheriff.config.ts, util/index.ts
- **Learnings for future iterations:**
  - Interface re-exports need `export type { }` when `isolatedModules: true` — test runner doesn't catch this but Angular build does
  - Sheriff needs 'root' dep rules for app files; add `'root': ['chairly-lib', 'shared']` to depRules
  - Library barrel at `libs/chairly/src` needs its own Sheriff tag — same pattern as 'shared' for shared-lib
  - `export type` changes don't break existing tests but fix Angular build's isolatedModules requirement
---

## 2026-03-02 - SCF-011
- ServiceFormDialogComponent created in libs/chairly/src/lib/services/ui/
- Uses native <dialog> element with viewChild.required<ElementRef<HTMLDialogElement>>('dialogEl')
- Signal inputs: categories (required), service (optional, null for create mode)
- OutputEmitterRef: saved (CreateServiceRequest | UpdateServiceRequest), cancelled (void)
- Reactive form with typed FormGroup: name, description, duration (minutes), price, categoryId, sortOrder
- open() resets the form (pre-populates in edit mode using parseDuration) and calls showModal()
- Duration converted from minutes to TimeSpan format (formatDurationToTimeSpan) on save
- Fixed 6 lint errors: all labels need `for="..."` attributes matching `id="..."` on form elements
- Fixed 1 test failure: <input type="number"> change events don't update Angular form controls in jsdom — use `input` events instead for number inputs in tests
- 10 unit tests covering: create, open, close, create mode reset, edit mode pre-populate, save emit with TimeSpan conversion, validation blocking submit, cancel, validation message display, category options
- Exported from ui/index.ts barrel
- Files changed: service-form-dialog.component.ts, service-form-dialog.component.spec.ts, ui/index.ts
- **Learnings for future iterations:**
  - All form `<label>` elements MUST have `for="id"` attributes matching `id="..."` on the associated input — `@angular-eslint/template/label-has-associated-control` enforces this
  - For `<input type="number">` in Angular tests (jsdom): dispatch `input` events, NOT `change` events — `change` events don't reliably update the form control value in jsdom even though Angular's NumberValueAccessor registers on `change`. Use `new Event('input')` instead.
---

## 2026-03-02 - SCF-012
- ServiceTableComponent created in libs/chairly/src/lib/services/ui/
- Signal inputs: services (input.required<ServiceResponse[]>), isLoading (input.required<boolean>)
- OutputEmitterRef events: editClicked, deleteClicked, toggleActiveClicked — all typed as ServiceResponse
- Table with columns: Name, Category, Duration (DurationPipe), Price (CurrencyPipe), Status badge, Actions
- Active badge: bg-green-100 / text-green-800; Inactive badge: bg-gray-100 / text-gray-600
- Toggle Active button shows 'Deactivate' for active services and 'Activate' for inactive ones (inline ternary — allowed since it's property access on signal result, not a function call)
- Loading state hides the table; empty state shows colspan="6" message row
- 14 unit tests covering: create, rows displayed, name/category/duration display, null category dash, active/inactive badges, loading state, empty state, editClicked/deleteClicked/toggleActiveClicked emits, activate/deactivate button text
- Exported from ui/index.ts barrel
- Files changed: service-table.component.ts, service-table.component.spec.ts, ui/index.ts
- **Learnings for future iterations:**
  - CurrencyPipe from @angular/common can be imported directly in standalone components — no CommonModule needed
  - Ternary in template for button text (e.g., `{{ service.isActive ? 'Deactivate' : 'Activate' }}`) is NOT a function call — it's a ternary expression on signal property access, so it passes no-call-expression rule
  - For colspan in HTML table cells: use `colspan="6"` (not Angular binding) for static values

## Codebase Patterns
- Sheriff requires an `index.ts` barrel in each tagged directory for proper module tagging (e.g., `data-access/index.ts`).
- Sheriff's tagging for `libs/shared/src` must be added as a direct entry (not under `libs/shared/src/lib`) so that barrel imports (`@org/shared-lib`) resolve to a tagged module.
- Sheriff uses AND logic for multi-tag dep rules — if a file has tags `[domain:X, layer:data-access]`, BOTH rules must allow the dependency. So `layer:data-access` must include `'shared'` in its allowed deps for data-access services to import from shared-lib.
- For `Observable<void>` delete methods in tests, flush with `req.flush(null)` and verify completion with a boolean flag (not `toBeUndefined()` — HttpClient returns null not undefined for empty bodies).
- Interface re-exports in barrel files need `export type { }` syntax when Angular build uses `isolatedModules: true`. Use `export type { MyInterface }` instead of `export { MyInterface }` for all TypeScript interfaces.
- Sheriff needs 'root' dep rules for app (apps/ directory) files, plus a 'chairly-lib' tag for `libs/chairly/src` barrel. Without these, Sheriff raises SH-002 errors.
- Smart container test pattern: mock stores as `{ signal(), vi.fn() }` plain objects, provide via TestBed, use `triggerEventHandler('eventName', payload)` to test event wiring.
- All form `<label>` elements MUST have `for="id"` attributes matching `id="..."` on the associated control — `@angular-eslint/template/label-has-associated-control` enforces this.
- For `<input type="number">` in Angular tests (jsdom): dispatch `input` events, NOT `change` events — `change` events don't reliably update the form control value in jsdom. Use `new Event('input')` instead.
- The git working directory is /home/jreuvers/projects/Chairly but `pwd` inside frontend tools resolves to /home/jreuvers/projects/Chairly/src/frontend/chairly. Use `git -C /home/jreuvers/projects/Chairly` for git commands.
- Frontend Nx quality checks are run from /home/jreuvers/projects/Chairly/src/frontend/chairly: `npx nx affected -t lint --base=main`, etc.
- npm install must be run once in src/frontend/chairly before Nx commands work (node_modules not committed).
- import-sort rule uses `simple-import-sort/imports` — Angular imports are sorted alphabetically by package name (@angular/common/http before @angular/core).
- SonarJS `no-nested-functions` enforces max 4 levels of function nesting — extract map/filter callbacks used inside patchState (which is inside subscribe which is inside withMethods) to module-level helper functions.
- signalStore() creates an injectable class — provide in TestBed; mocked dependencies via { provide: X, useValue: mockObj }; of() is synchronous so no fakeAsync needed.
- eslint --fix auto-sorts imports/exports — run it after creating files to avoid manual sort guessing.
- `@angular-eslint/template/no-call-expression` does NOT support Angular signal reads — it blocks all function calls including `signal()` reads. It has been disabled in eslint.config.mjs (rule source confirmed no TypeScript type checking). Signal reads in templates remain the approved Angular 17+ pattern.
- Angular's HTML parser (used by @angular-eslint) treats `<dialog>` as having implied end tags in some versions — the `Parsing error: '>' expected` at line 18:6 was a misleading error from a `</dialog>` closing tag mismatch (opening tag was accidentally changed to `<div>` but closing tag left as `</dialog>`). Always use matching `<dialog>...</dialog>` tags.
- Two separate `import ... from '@angular/core'` statements cause `simple-import-sort/imports` errors and `Parsing error: '>' expected` in the inline template processor. Always merge all Angular core imports into one statement.
- Angular signal inputs (`input()`, `input.required()`) and outputs (`output()`) must be marked `readonly` — `@angular-eslint/prefer-signals` enforces this. Run `eslint --fix` to auto-fix.
- For testing Angular components with native `<dialog>` element: jsdom doesn't implement `showModal()` or `close()`. Assign mock functions directly: `dialogEl.showModal = vi.fn(); dialogEl.close = vi.fn();` after `fixture.detectChanges()`.
- Signal outputs (`output<T>()` returns `OutputEmitterRef<T>`) have a `.subscribe()` method usable in tests: `component.output.subscribe(() => { done = true; })`.
- `viewChild.required<ElementRef<HTMLDialogElement>>('name')` requires the template reference variable name to match the string parameter. Use a non-conflicting name like `#dialogEl` (not `#dialog` which may cause issues).

## 2026-03-02 - SCF-013
- ServiceListPageComponent created in libs/chairly/src/lib/services/feature/
- Smart container: injects ServiceStore and ServiceCategoryStore (both private, accessed via computed properties exposed as protected)
- On init, calls loadServices() and loadCategories() on both stores
- Template: header with "Add Service" button, main area with ServiceTableComponent, sidebar with CategoryPanelComponent
- Three dialogs in template: ServiceFormDialogComponent (shared for create/edit), two ConfirmationDialogComponents (#deleteServiceDialog, #deleteCategoryDialog)
- viewChild.required(ServiceFormDialogComponent) for single form dialog (type-based query)
- viewChild.required<ConfirmationDialogComponent>('deleteServiceDialog') and 'deleteCategoryDialog' for string-ref queries
- signal<ServiceResponse | null>(null) for selectedService; signal<string | null>(null) for selectedCategoryIdForDelete
- Protected computed signals (services, servicesLoading, categories, categoriesLoading) wrap store signals
- 14 unit tests: create, loadServices/loadCategories on init, Add Service button opens dialog, edit mode, create/update service via saved event, delete service confirmation flow, toggleActive, category create/update/delete
- Created feature/index.ts barrel exporting ServiceListPageComponent
- All lint + test + build checks pass
- Files changed: service-list-page.component.ts, service-list-page.component.spec.ts, feature/index.ts
- **Learnings for future iterations:**
  - `npx eslint` run directly on a component file may report `Parsing error: '>' expected` at the template start — this is a false positive only seen when running ESLint directly (not via `npx nx`). The `npx nx affected -t lint --base=main` command correctly reports no errors.
  - viewChild.required(ComponentType) (type-based) works for single component; use viewChild.required<T>('templateRefVarName') for multiple same-type components
  - For smart container tests: mock stores as plain objects with signal() state and vi.fn() methods; provide them via { provide: StoreClass, useValue: mockObj } in TestBed
  - triggerEventHandler() on debugElement for event bindings is the clean way to test event wiring in smart containers
  - SonarJS `no-undefined-argument` flags `triggerEventHandler('event', undefined)` — use `null` instead
---

## 2026-02-27 - SCF-001
- Fixed ESLint prefix from 'lib' to 'chairly' in libs/chairly/eslint.config.mjs and libs/shared/eslint.config.mjs (both directive and component selector rules)
- Added provideHttpClient() to apps/chairly/src/app/app.config.ts with correct import from @angular/common/http
- Fixed import order (simple-import-sort requires @angular/common/http before @angular/core alphabetically)
- Files changed: libs/chairly/eslint.config.mjs, libs/shared/eslint.config.mjs, apps/chairly/src/app/app.config.ts
- **Learnings for future iterations:**
  - Node modules not installed initially — run `npm install` in src/frontend/chairly first
  - Git commands must use absolute path: `git -C /home/jreuvers/projects/Chairly`
  - simple-import-sort enforces alphabetical order within Angular import group
  - Working directory for Nx commands is /home/jreuvers/projects/Chairly/src/frontend/chairly (already set as default)
---

## 2026-03-02 - SCF-003
- Created TypeScript interfaces matching backend DTOs in libs/chairly/src/lib/services/util/
- service.models.ts: ServiceResponse, CreateServiceRequest, UpdateServiceRequest
- service-category.models.ts: ServiceCategoryResponse, CreateServiceCategoryRequest, UpdateServiceCategoryRequest
- index.ts: barrel file exporting all interfaces
- Files changed: libs/chairly/src/lib/services/util/service.models.ts, libs/chairly/src/lib/services/util/service-category.models.ts, libs/chairly/src/lib/services/util/index.ts
- **Learnings for future iterations:**
  - Interfaces were pre-scaffolded by prior work — always check if files already exist before creating new ones
  - GUIDs → string, TimeSpan → string ('HH:MM:SS'), DateTimeOffset → string (ISO 8601)
  - The services/util/index.ts barrel exports all interfaces for consumption by data-access and feature layers
---

## 2026-03-02 - SCF-004
- Created ServiceCategoryApiService in libs/chairly/src/lib/services/data-access/ with inject(HttpClient) and inject(API_BASE_URL)
- Methods: getAll() → GET, create() → POST, update() → PUT, delete() → DELETE on /api/service-categories
- Created data-access/index.ts barrel (required for Sheriff module tagging)
- Created unit tests with provideHttpClientTesting() and HttpTestingController; 5 tests verify each method's URL, HTTP verb, and body
- Updated sheriff.config.ts: added 'libs/shared/src': ['shared'] tag for barrel imports, added 'shared' to layer:feature/ui/data-access dep rules
- Files changed: service-category-api.service.ts, service-category-api.service.spec.ts, data-access/index.ts, sheriff.config.ts
- **Learnings for future iterations:**
  - Sheriff requires index.ts barrel in each tagged directory for module detection
  - Sheriff's `libs/shared/src` must be tagged directly (for barrel @org/shared-lib imports)
  - Sheriff uses AND logic: all tags must allow a dependency — so layer:data-access MUST include 'shared'
  - Observable<void> tests: use completion flag instead of toBeUndefined() (HttpClient returns null not undefined)
---

## 2026-03-02 - SCF-005
- Created ServiceApiService in libs/chairly/src/lib/services/data-access/
- Methods: getAll() → GET, getById() → GET, create() → POST, update() → PUT, delete() → DELETE, toggleActive() → PATCH on /api/services
- toggleActive uses PATCH /api/services/{id}/toggle-active with null body
- Created unit tests (service-api.service.spec.ts) with 7 tests covering all methods
- Updated data-access/index.ts barrel to export ServiceApiService
- Files changed: service-api.service.ts, service-api.service.spec.ts, data-access/index.ts
- **Learnings for future iterations:**
  - PATCH with no request body: pass `null` as the body in `http.patch<T>(url, null)`
  - Same patterns as SCF-004 (ServiceCategoryApiService) work cleanly for services
  - For Observable<void> delete tests, use completion flag pattern (req.flush(null))
---

## 2026-03-02 - SCF-006
- Created ServiceCategoryStore in libs/chairly/src/lib/services/data-access/service-category.store.ts
- State shape: categories (ServiceCategoryResponse[]), isLoading (boolean), error (string | null)
- Methods: loadCategories, createCategory, updateCategory, deleteCategory — all using direct subscribe with take(1)
- Extracted module-level helper functions (toErrorMessage, replaceCategory, removeCategory) to avoid SonarJS no-nested-functions violations (max 4 levels deep)
- Unit tests in service-category.store.spec.ts — 9 tests covering initial state, success and error paths for all 4 methods
- Updated data-access/index.ts to export ServiceCategoryStore and ServiceCategoryState
- Auto-fixed import/export sort with eslint --fix
- Files changed: service-category.store.ts, service-category.store.spec.ts, data-access/index.ts
- **Learnings for future iterations:**
  - SonarJS no-nested-functions limits nesting to 4 levels deep — withMethods + method + subscribe.next + patchState = 4 levels, so any callback inside patchState (map, filter) must be extracted to module-level helpers
  - signalStore() from @ngrx/signals creates an injectable class — provide it in TestBed.configureTestingModule with { provide: ServiceCategoryApiService, useValue: mockService }
  - With vitest globals: true, vi.fn() is available without imports in spec files
  - Use of(value) for synchronous mock observables — state updates happen synchronously so no fakeAsync needed
  - simple-import-sort sorts exports: run eslint --fix to auto-sort rather than guessing the order
  - ServiceCategoryStore is NOT providedIn root — it will be provided at route level in SCF-014
---

## 2026-03-02 - SCF-007
- Created ServiceStore in libs/chairly/src/lib/services/data-access/service.store.ts
- State shape: services (ServiceResponse[]), isLoading (boolean), error (string | null)
- Computed signals: activeServices (filters by isActive) and servicesByCategory (groups by categoryId, 'uncategorized' for null)
- Methods: loadServices, createService, updateService, deleteService, toggleActive — all using direct subscribe with take(1)
- groupByCategory extracted as module-level function to avoid SonarJS no-nested-functions violations
- Unit tests in service.store.spec.ts — 16 tests covering initial state, computed signals, and all 5 methods (success + error paths)
- Updated data-access/index.ts barrel to export ServiceStore and ServiceState
- Auto-fixed import/export sort with eslint --fix (index.ts reordered by linter)
- Files changed: service.store.ts, service.store.spec.ts, data-access/index.ts
- **Learnings for future iterations:**
  - withComputed receives the store and returns an object of Angular computed() signals — import computed from @angular/core
  - groupByCategory uses a for..of loop (not Array.reduce) to stay at ≤4 nesting levels for SonarJS
  - Computed signals are exposed as signal accessors: store.activeServices(), store.servicesByCategory()
  - Same signalStore patterns as ServiceCategoryStore apply cleanly for services
---

## 2026-02-27 - SCF-002
- Created apps/chairly/proxy.conf.json with /api → http://localhost:5205 proxy config (changeOrigin: true)
- Updated apps/chairly/project.json serve target with proxyConfig option pointing to proxy.conf.json
- Created libs/shared/src/lib/util/api-base-url.token.ts with InjectionToken<string> defaulting to '/api'
- Exported API_BASE_URL from libs/shared/src/index.ts (barrel)
- Files changed: apps/chairly/proxy.conf.json, apps/chairly/project.json, libs/shared/src/lib/util/api-base-url.token.ts, libs/shared/src/index.ts
- **Learnings for future iterations:**
  - InjectionToken with providedIn: 'root' factory is the clean pattern — no need to provide in any module
  - The shared barrel index.ts was empty (single blank line) — write directly with the exports
  - proxyConfig in project.json goes in serve.options (not in configurations)
---

## 2026-03-02 - SCF-009
- Created parseDuration(timeSpan: string): number in services/util/duration.utils.ts — converts 'HH:MM:SS' to total minutes
- Created formatDurationToTimeSpan(minutes: number): string — converts minutes back to 'HH:MM:SS' format
- Created DurationPipe (name: 'duration') in services/util/duration.pipe.ts — formats TimeSpan strings as '30 min', '1h', '1h 30min'
- Created unit tests: duration.utils.spec.ts (11 tests) and duration.pipe.spec.ts (7 tests)
- Updated services/util/index.ts barrel to export DurationPipe, parseDuration, formatDurationToTimeSpan
- Files changed: duration.utils.ts, duration.pipe.ts, duration.utils.spec.ts, duration.pipe.spec.ts, util/index.ts
- **Learnings for future iterations:**
  - Angular Pipes can be tested directly without TestBed — just instantiate the class and call transform()
  - DurationPipe display logic: hours=0 → '{min} min', minutes=0 → '{h}h', both → '{h}h {min}min'
  - parseDuration only needs hours and minutes parts (index 0 and 1) from the HH:MM:SS split — seconds are ignored
---

## 2026-03-02 - SCF-010
- Created CategoryPanelComponent in libs/chairly/src/lib/services/ui/category-panel.component.ts
- Signal inputs: categories (input.required<ServiceCategoryResponse[]>), isLoading (input.required<boolean>)
- OutputEmitterRef events: categoryCreated, categoryUpdated ({id, request}), categoryDeleted (string)
- Protected computed signal: sortedCategories — sorts categories by sortOrder
- Protected signals: showAddForm, editingCategoryId — for inline UI state
- Two FormGroups (addForm, editForm) using ReactiveFormsModule with Validators.required and Validators.maxLength(150)
- Inline add form shown via @if (showAddForm()) in header area
- Inline edit form shown via @if (editingCategoryId() === category.id) per row
- @for with @empty for category list; @if (!isLoading()) for list visibility
- Created 14 unit tests covering: create, sorted display, loading state, add/cancel flow, categoryCreated emit, edit/cancel flow, categoryUpdated emit, categoryDeleted emit, empty state
- Created ui/index.ts barrel exporting CategoryPanelComponent
- Auto-fixed import sort with eslint --fix on spec file
- Files changed: category-panel.component.ts, category-panel.component.spec.ts, ui/index.ts
- **Learnings for future iterations:**
  - Use signal<boolean>(false) and signal<string | null>(null) for local UI state in presentational components (showAddForm, editingCategoryId)
  - Two separate FormGroups (addForm, editForm) work cleanly for inline add + edit patterns
  - Template comparison `editingCategoryId() === category.id` inside @if works correctly with OnPush — just a signal read + property access
  - title="Edit category" / title="Delete category" attributes on buttons make test querying clean with querySelector('[title="..."]')
  - sortedCategories computed signal: `[...this.categories()].sort(...)` — spread to avoid mutating the input array
  - Always run `eslint --fix` on spec files after creation to auto-sort imports
---

## 2026-03-02 - SCF-008
- Created ConfirmationDialogComponent in libs/shared/src/lib/ui/confirmation-dialog.component.ts
- Uses native <dialog> element with showModal()/close() via viewChild.required<ElementRef<HTMLDialogElement>>('dialogEl')
- Signal inputs: title (required), message (required), confirmLabel (default 'Confirm'), cancelLabel (default 'Cancel'), isDestructive (default false)
- OutputEmitterRef events: confirmed, cancelled — both marked readonly
- Protected computed signal: confirmButtonClass — returns red styling when isDestructive, indigo otherwise
- Protected onConfirm()/onCancel() methods close dialog and emit events
- Created 9 unit tests in confirmation-dialog.component.spec.ts: create, open, close, confirm emit, cancel emit, default labels, custom labels, destructive styling, default styling
- Created libs/shared/src/lib/ui/index.ts barrel and updated libs/shared/src/index.ts to export ConfirmationDialogComponent
- Updated eslint.config.mjs to disable `@angular-eslint/template/no-call-expression` (rule does not support Angular 17+ signal reads)
- Files changed: confirmation-dialog.component.ts, confirmation-dialog.component.spec.ts, libs/shared/src/lib/ui/index.ts, libs/shared/src/index.ts, eslint.config.mjs
- **Learnings for future iterations:**
  - The `no-call-expression` ESLint rule (v21) does NOT detect Angular signals — it blocks ALL function calls including signal reads. It must be disabled for Angular 17+ signal-based projects. The rule source shows it uses allowList/allowPrefix/allowSuffix options, no TypeScript type detection.
  - Two imports from the same module cause parse errors with the inline template processor — merge all @angular/core imports into one statement.
  - jsdom doesn't implement HTMLDialogElement.showModal()/close() — assign vi.fn() directly to the element after detectChanges().
  - Template reference variables (#dialogEl, not #dialog) — avoid names that conflict with HTML element names to prevent potential parsing issues.
  - Mark all signal inputs/outputs/computed as `readonly` — the `@angular-eslint/prefer-signals` rule enforces this.
---
